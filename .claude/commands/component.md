Create a new React component for: $ARGUMENTS

Requirements:
- TypeScript with explicit prop interface
- Use forwardRef if it wraps a DOM element
- Use cn() from @/lib/cn for conditional classNames
- Use Tailwind CSS only, no separate CSS files
- Include keyboard interaction if interactive
- Export as default
- Place in the appropriate directory under src/components/